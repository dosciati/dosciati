# Repositórios (gerado automaticamente)

Esta página será **gerada automaticamente** pela *GitHub Action* em cada push.

Se preferir visualizar agora, rode localmente:
```bash
pip install -r requirements.txt
mkdocs serve
```